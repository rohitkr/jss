// @flow
import type {JssOptions} from '@secure-jss/jss'
import {type Options as DefaultUnitOptions} from '@secure-jss/jss-plugin-default-unit'
import {type Options as ObservableOptions} from '@secure-jss/jss-plugin-rule-value-observable'

type Create = (
  {|
    defaultUnit?: DefaultUnitOptions,
    observable?: ObservableOptions
  |} | void
) => JssOptions

declare export default Create
